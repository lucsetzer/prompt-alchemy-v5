[build]
builder = "nixpacks"

[deploy]
numReplicas = 1

[[services]]
name = "prompt-alchemy"
port = 8000
startCommand = "uvicorn main:app --host 0.0.0.0 --port ${PORT}"

[variables]
PORT = "8000"
# Add other env vars here if needed